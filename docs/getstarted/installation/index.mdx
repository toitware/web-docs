import LinuxIcon from "../../_images/linux-brands.svg";
import WindowsIcon from "../../_images/windows-brands.svg";
import MacOSIcon from "../../_images/apple-brands.svg";

# Install development environment

Download and install the Toit command-line interface (CLI) and the Visual Studio Code extension for Toit for setting up your local development environment.

Choose the relevant platform.

<Boxes>

<Box to="./windows" title="Toit for Windows" imageUrl={WindowsIcon} />

<Box to="./macos" title="Toit for macOS" imageUrl={MacOSIcon} />

<Box to="./linux" title="Toit for Linux" imageUrl={LinuxIcon} />

</Boxes>
