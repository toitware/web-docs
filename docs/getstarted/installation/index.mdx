import LinuxIcon from "../../_images/linux-brands.inline.svg";
import WindowsIcon from "../../_images/windows-brands.inline.svg";
import MacOSIcon from "../../_images/apple-brands.inline.svg";

# Install development environment

Download and install the Toit command-line interface (CLI) and the Visual Studio Code extension for Toit for setting up your local development environment.

Choose the relevant platform.

<Boxes>

<Box to="./windows" title="Toit for Windows" image={<WindowsIcon />} />

<Box to="./macos" title="Toit for macOS" image={<MacOSIcon />} />

<Box to="./linux" title="Toit for Linux" image={<LinuxIcon />} />

</Boxes>
